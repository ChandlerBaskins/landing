import { DegreesToDirectionPipe } from './degrees-to-direction.pipe';

describe('DegreesToDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new DegreesToDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
